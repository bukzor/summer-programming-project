---
depends:
    - ../claims.kb/session-zero-agenda.md
candidate-resolutions:
    - ../claims.kb/abby-chose-crafting-mechanic.md
sources:
    - ../sources.kb/buck.md
    - ../sources.kb/abby.md
tags: [session-zero]
---

What game should she actually build (genre, character, win condition)?

Mechanic is answered: crafting (`abby-chose-crafting-mechanic.md`). Genre
wrapper, character, and win condition were never answered live — the
build in `../curriculum.kb/` went ahead without them: genre wrapper
settled implicitly as a Minecraft-like sandbox, character is punted to
`../curriculum.kb/design/070-future-work.kb/player-character.md`, and the
core arc (sessions 0-7) has no explicit win condition at all, just a
mine→craft→gate→build loop. Not necessarily wrong — a sandbox with no win
state may suit her fine — but it means these agenda items got decided by
default rather than by her, which is the opposite of what
`session-zero-agenda.md` was for.

Still open as of the 2026-07-11 rewrite of `session-zero-agenda.md`: the
Minecraft-sandbox frame and no-win-condition default are explicitly named
as her call to confirm or override in session 0, not settled facts —
`../claims.kb/session-zero-agenda.md` item 2. Character remains genuinely
deferred (no player character exists yet in the core arc,
`../curriculum.kb/design/070-future-work.kb/player-character.md`), so
there's nothing to confirm there yet beyond letting her imagine one if she
wants to.
