---
why:
  - ../020-goals.kb/every-session-ends-runnable.md
---

At the end of every session: `uv run python main.py` opens without error,
and there is exactly one new git commit whose message Abby dictated.
Verified trivially — run it, check `git log -1`.
