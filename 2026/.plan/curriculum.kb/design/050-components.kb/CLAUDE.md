Concrete functions and data structures — the pieces `main.py` is actually
made of. Responsibility, interface, and representation choices; not full
listings (the code is the listing). Add an entry when a design abstraction
gets a concrete name in `main.py`.
