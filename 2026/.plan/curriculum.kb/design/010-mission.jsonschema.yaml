# yaml-language-server: $schema=https://json-schema.org/draft-07/schema
$schema: "http://json-schema.org/draft-07/schema#"
description: >
  What problem this project solves, who benefits, and what success looks
  like. Top of the why/how tower — no why[] link (nothing motivates the
  mission but the mission itself).
type: object
additionalProperties: false
properties:
  tags:
    description: Topic tags for cross-referencing.
    type: array
    items:
      type: string
