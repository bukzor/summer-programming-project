---
force: must
why:
  - ../design/040-design.kb/world-representation.md
---

`WIDTH = 800`, `HEIGHT = 600`, `TILE = 40`. World grid: `GRID_COLS = 20`,
`GRID_ROWS = 12` (480px). The bottom 120px is the UI strip — reserved from
session 0 (drawn as a dark panel) so inventory/crafting later needs no
layout refactor.
