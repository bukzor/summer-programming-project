Scratch autosaves constantly — "save, then run" is not a concept she has.
The most confusing non-error in her future: "I changed it and ran it and
nothing changed" — an unsaved buffer. Name it in session 0, make `:w` the
first beat of the run ritual (see
`../../../technical-policy.kb/vim-and-tmux-setup.md`), and when it bites
anyway, the first question is always "did you save?". Deliberately not
automated away with autosave: *the saved file is what runs* is curriculum
content, not friction.
