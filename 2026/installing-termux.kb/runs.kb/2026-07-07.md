---
date: 2026-07-07
outcome: abandoned
reached: 01-install-apks.md
duration-minutes: 60
---

Attempt 1. Stopped mid-attempt, preempted -- Abby started using the tablet
partway through step 02, and further steps either need her keyboard input
directly (step 02.1) or would foreground-hijack the device via adb input
injection, which shouldn't happen while she's on it (see "Operating note"
in `../CLAUDE.md`, added this session). Not blocked by a bug -- picking
this back up just needs the tablet free.

Versions installed: Termux `0.118.3` (build 1002, F-Droid stable, not the
0.119.0-beta.3 also listed); Termux:X11 `nightly`
(2026-07-07T13:47:58Z, commit `aa74aac`, package `1.03.01-0`,
`app-universal-debug.apk`).

Deviations from steps as written, patched back into `../steps.kb/`:

- Two adb transports showed up for the same tablet (`ip:port` +
  `adb-tls-connect` mDNS); had to target `-s <ip:port>` explicitly on
  every command. Patched into `00-preflight.md`.
- Android version read `16`, not the `15` on record
  (`../../claims.kb/hardware-is-android-tablet.md`, updated). Tablet took
  an OS update since hardware profiling.
- Termux install hit `INSTALL_FAILED_VERIFICATION_FAILURE` (Play Protect
  blocking the sideloaded F-Droid APK); Termux:X11 install did not hit
  this. New troubleshooting entry:
  `../troubleshooting.kb/install-failed-verification-failure.md`.
- Battery-optimization exemption (deferred from step 00.3) has an adb
  path (`dumpsys deviceidle whitelist +<pkg>`) that needs no on-device
  Settings navigation; step 01.4 now offers this as the primary method.

Everything through step 01 verified green: model/version confirmed,
phantom-process killer disabled and reads back correctly, both APKs
installed (`pm list packages` lists both), battery exemption applied
(`dumpsys deviceidle whitelist` lists both).
