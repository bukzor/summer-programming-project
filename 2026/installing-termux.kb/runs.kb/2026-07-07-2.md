---
date: 2026-07-07
outcome: partial
reached: 02-bootstrap-termux.md
duration-minutes: 144
---

Attempt 2. Continued from attempt 1's step-02 pause. Mid-attempt, a Play
Store install of a Termux add-on nudged core Termux onto the Play Store
too (`../troubleshooting.kb/play-store-termux-family-trap.md`); reinstalled
via F-Droid (`com.termux` `0.118.3` build 1002, new UID `u0_a346`), which
also reset ssh access -- re-established pubkey-only (no password auth).

Versions: `python` `3.14.6-1`, `python-pygame` `2.6.1-5` (tur-repo,
initially ABI-mismatched against a stale `python`; resolved by a full
`pkg update && pkg upgrade` post-reinstall, no source build needed),
`pgzero` `1.2.1` in a directory-scoped `uv` venv
(`--system-site-packages`, not system-wide pip).

Deviations patched into `../steps.kb/`: 01 (Play Store family trap,
Termux:Styling), 02 (pgzero via `uv` venv, `sv-enable` service-directory
gotcha), 04/06 (venv-scoped `python`/`pgzrun` paths). New troubleshooting
entries: `sv-enable-service-directory-missing.md`,
`pygame-python-abi-mismatch.md`, `play-store-termux-family-trap.md`.

Everything through step 02 verified green: repos, X11 packages,
`python -c "import pygame"`, venv `python -c "import pgzero, pygame"`.

Not yet attempted: step 03 (first X11 display) onward.
