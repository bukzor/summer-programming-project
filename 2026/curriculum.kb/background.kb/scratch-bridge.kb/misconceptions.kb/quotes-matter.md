In Scratch you type text into a slot and it just works. In Python,
`"dirt"` is the word and `dirt` is a variable name — forgetting quotes gives
a NameError. Expect this repeatedly in session 2; greet it like an old
friend, not a setback.
