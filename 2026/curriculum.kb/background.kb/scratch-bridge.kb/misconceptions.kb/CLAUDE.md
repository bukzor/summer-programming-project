Places Scratch knowledge actively misleads — name each BEFORE she trips on
it, per session tie-in noted in the file. These are guaranteed wrong
predictions, useful on purpose (see
`../../../design/040-design.kb/deliberate-bug-pedagogy.md`). Add a file when
a new Scratch→Python transfer failure is identified; each should name the
session where it first bites.
