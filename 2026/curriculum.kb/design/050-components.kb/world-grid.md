---
why:
  - ../040-design.kb/world-representation.md
---

`world`, a list of `GRID_ROWS` row-lists of `GRID_COLS` block-name strings
(`world[row][col]`), built by loops in session 1–2 and then hand-edited for
trees/diamonds/her invented block. Never a 240-cell literal — building it in
loops is itself the session-1/2 content.
