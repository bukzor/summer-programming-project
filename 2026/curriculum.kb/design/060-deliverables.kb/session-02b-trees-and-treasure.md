---
why:
  - ../050-components.kb/world-grid.md
status: not-started
---

**Goal:** plant a forest, bury treasure, make one block hers. Pure play —
zero new concepts, all hand-editing the `world` list built in session 2a.

**Build:** hand-edit `world`: tree trunks (`"wood"` columns) topped with
`"leaves"`, bury `"diamond"` in the stone. She invents one new block type
(name + color), adds it to `COLORS`, and places it somewhere in the world —
canon from here on.

**Concepts:** none new — this session is reps on 2a's list-of-lists
indexing, applied by hand instead of by loop.

**Done when:** at least one tree and one hidden diamond exist, plus her
invented block placed somewhere in the world.

**Pitfall:** still edits specific `world[row][col]` assignments, never a
240-cell retyped literal.
