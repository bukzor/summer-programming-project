Verifiable statements that check whether the goals are actually being met —
things you could grep the code or watch a session for. Add an entry when a
goal implies something checkable that isn't checkable yet.
