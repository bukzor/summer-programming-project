Propositional assertions — both plain observations (used as deduction
premises) and recommendations (used as question candidate-resolutions). A
recommendation being `status: asserted` means the assistant is confident in
it, not that the user has accepted it.
