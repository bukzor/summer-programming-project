Open inquiries awaiting the user's decision. `candidate-resolutions` points
to the assistant's recommended claim; the question stays open (no
`resolved:`) until the user picks one or rejects all of them. Once resolved,
promote the outcome into `../../design/` or `../../technical-policy.kb/` and
remove or archive the question.
