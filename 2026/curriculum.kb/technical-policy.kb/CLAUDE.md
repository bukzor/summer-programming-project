Cross-cutting normative rules for `main.py` — means, not ends; fixed up
front so no later session forces a rewrite. Each links `why:` to the
design/goal it serves via `../design/`. Do not deviate from a `force: must`
entry without discussing it first — that's what the old CURRICULUM.md meant
by "Sonnet: do not deviate."
